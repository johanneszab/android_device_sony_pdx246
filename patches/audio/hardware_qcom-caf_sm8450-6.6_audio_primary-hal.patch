diff --git a/adsprpcd/Android.bp b/adsprpcd/Android.bp
index 8847f2e6f..40c559929 100644
--- a/adsprpcd/Android.bp
+++ b/adsprpcd/Android.bp
@@ -1,4 +1,10 @@
 cc_binary {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "audioadsprpcd",
 
     srcs: ["adsprpcd.c"],
diff --git a/hal/converter/Android.bp b/hal/converter/Android.bp
index afaa66159..2260d144c 100644
--- a/hal/converter/Android.bp
+++ b/hal/converter/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudioplatformconverter.qti",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/core/Android.bp b/hal/core/Android.bp
index b611e2e4f..f5f73ca0c 100644
--- a/hal/core/Android.bp
+++ b/hal/core/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudiocorehal.qti",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
@@ -83,6 +89,12 @@ cc_library_shared {
 }
 
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudiocore.extension",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
@@ -132,6 +144,12 @@ cc_library_static {
 }
 
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudioplatform.qti",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/core/extensions/Android.bp b/hal/core/extensions/Android.bp
index af5befa0e..56d6fdbc5 100644
--- a/hal/core/extensions/Android.bp
+++ b/hal/core/extensions/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libhfp_pal",
     vendor: true,
 
@@ -44,6 +50,12 @@ cc_library_shared {
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libfmpal",
     vendor: true,
 
@@ -81,6 +93,12 @@ cc_library_shared {
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libbatterylistener",
     vendor: true,
 
diff --git a/hal/core/include/qti-audio-core/Stream.h b/hal/core/include/qti-audio-core/Stream.h
index 40bb69f37..ff776c765 100644
--- a/hal/core/include/qti-audio-core/Stream.h
+++ b/hal/core/include/qti-audio-core/Stream.h
@@ -505,6 +505,23 @@ class StreamCommonDelegator : public ::aidl::android::hardware::audio::core::BnS
         return delegate != nullptr ? delegate->removeEffect(in_effect)
                                    : ndk::ScopedAStatus::fromExceptionCode(EX_ILLEGAL_STATE);
     }
+    // android.hardware.audio.core V4 added IStreamCommon::createMmapBuffer().
+    // QTI wrote this HAL against V2 (see hal/default/manifest_audiocorehal_default.xml),
+    // so without this override StreamCommonDelegator stays abstract and
+    // SharedRefBase::make<StreamCommonDelegator>() in Stream.cpp fails to compile.
+    //
+    // Returning EX_UNSUPPORTED_OPERATION is exactly what AOSP's own default HAL
+    // does (StreamCommonImpl::createMmapBuffer in
+    // hardware/interfaces/audio/aidl/default/Stream.cpp), and the interface
+    // documents that as the contract for a non-MMap stream. QTI's MMAP support
+    // goes through its own configureMMapStream() path, which is untouched.
+    ndk::ScopedAStatus createMmapBuffer(
+            ::aidl::android::hardware::audio::core::MmapBufferDescriptor* _aidl_return)
+            override {
+        (void)_aidl_return;
+        return ndk::ScopedAStatus::fromExceptionCode(EX_UNSUPPORTED_OPERATION);
+    }
+
     // It is possible that on the client side the proxy for IStreamCommon will
     // outlive the IStream* instance, and the server side IStream* instance will
     // get destroyed while this IStreamCommon instance is still alive.
diff --git a/hal/core/manifest_audiocoreservices_qti.xml b/hal/core/manifest_audiocoreservices_qti.xml
index b5d5e84d3..1b184fc0b 100644
--- a/hal/core/manifest_audiocoreservices_qti.xml
+++ b/hal/core/manifest_audiocoreservices_qti.xml
@@ -2,25 +2,30 @@
 Copyright (c) 2023-2024 Qualcomm Innovation Center, Inc. All rights reserved.
 SPDX-License-Identifier: BSD-3-Clause-Clear
 -->
+<!-- device/sony/pdx246: android.hardware.audio.core bumped 2 -> 4. QTI wrote this HAL
+     against V2; LineageOS 23.2 ships V4 (see the AOSP apex manifest
+     hardware/interfaces/audio/aidl/default/android.hardware.audio.service-aidl.xml,
+     which declares 4). A stale version here means the framework never
+     finds the service. -->
 <manifest version="1.0" type="device">
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/default</fqname>
   </hal>
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/r_submix</fqname>
   </hal>
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/usb</fqname>
   </hal>
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IConfig/default</fqname>
   </hal>
 </manifest>
diff --git a/hal/core/module_config/Android.bp b/hal/core/module_config/Android.bp
index 3f310815c..237d960ad 100644
--- a/hal/core/module_config/Android.bp
+++ b/hal/core/module_config/Android.bp
@@ -1,4 +1,10 @@
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudio_module_config.qti",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/core/platform/microphoneInfoParser/Android.bp b/hal/core/platform/microphoneInfoParser/Android.bp
index ccce4ad3d..502a79c0d 100644
--- a/hal/core/platform/microphoneInfoParser/Android.bp
+++ b/hal/core/platform/microphoneInfoParser/Android.bp
@@ -1,4 +1,10 @@
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudio_microphoneinfo_parser",
     defaults: [
         "latest_android_media_audio_common_types_ndk_shared",
diff --git a/hal/core/utils/Android.bp b/hal/core/utils/Android.bp
index dbe7880bc..20ab47b9f 100644
--- a/hal/core/utils/Android.bp
+++ b/hal/core/utils/Android.bp
@@ -1,4 +1,10 @@
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudiohalutils.qti",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/default/Android.bp b/hal/default/Android.bp
index b2241f973..0c251180e 100644
--- a/hal/default/Android.bp
+++ b/hal/default/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudiocorehal.default",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/default/manifest_audiocorehal_default.xml b/hal/default/manifest_audiocorehal_default.xml
index 28abe4cc4..49a643466 100644
--- a/hal/default/manifest_audiocorehal_default.xml
+++ b/hal/default/manifest_audiocorehal_default.xml
@@ -2,29 +2,34 @@
 Copyright (c) 2024 Qualcomm Innovation Center, Inc. All rights reserved.
 SPDX-License-Identifier: BSD-3-Clause-Clear
 -->
+<!-- device/sony/pdx246: android.hardware.audio.core bumped 2 -> 4. QTI wrote this HAL
+     against V2; LineageOS 23.2 ships V4 (see the AOSP apex manifest
+     hardware/interfaces/audio/aidl/default/android.hardware.audio.service-aidl.xml,
+     which declares 4). A stale version here means the framework never
+     finds the service. -->
 <manifest version="1.0" type="device">
 
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IConfig/default</fqname>
   </hal>
 
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/default</fqname>
   </hal>
 
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/r_submix</fqname>
   </hal>
 
   <hal format="aidl">
     <name>android.hardware.audio.core</name>
-    <version>2</version>
+    <version>4</version>
     <fqname>IModule/usb</fqname>
   </hal>
 </manifest>
diff --git a/hal/effects/Android.bp b/hal/effects/Android.bp
index aaaca52bd..14bb0c2a6 100644
--- a/hal/effects/Android.bp
+++ b/hal/effects/Android.bp
@@ -31,6 +31,12 @@ cc_defaults {
 }
 
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudioeffecthal_base_impl_static",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
@@ -48,6 +54,12 @@ cc_library_static {
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudioeffecthal.qti",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
@@ -74,6 +86,12 @@ cc_library_shared {
 }
 
 cc_library_headers {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libaudioeffectsaidlqti_headers",
     export_include_dirs: ["include"],
     vendor: true,
diff --git a/hal/effects/audioeffectservice_qti.xml b/hal/effects/audioeffectservice_qti.xml
index c3cfe7e66..c0a3d6e8e 100644
--- a/hal/effects/audioeffectservice_qti.xml
+++ b/hal/effects/audioeffectservice_qti.xml
@@ -2,10 +2,15 @@
 Copyright (c) 2023-2024 Qualcomm Innovation Center, Inc. All rights reserved.
 SPDX-License-Identifier: BSD-3-Clause-Clear
 -->
+<!-- device/sony/pdx246: android.hardware.audio.effect bumped 2 -> 3. QTI wrote this HAL
+     against V2; LineageOS 23.2 ships V3 (see the AOSP apex manifest
+     hardware/interfaces/audio/aidl/default/android.hardware.audio.service-aidl.xml,
+     which declares 3). A stale version here means the framework never
+     finds the service. -->
 <manifest version="1.0" type="device">
   <hal format="aidl">
     <name>android.hardware.audio.effect</name>
-    <version>2</version>
+    <version>3</version>
     <fqname>IFactory/default</fqname>
   </hal>
 </manifest>
diff --git a/hal/effects/qcom-effects/offloadbundle/Android.bp b/hal/effects/qcom-effects/offloadbundle/Android.bp
index bb272e99a..722883b42 100644
--- a/hal/effects/qcom-effects/offloadbundle/Android.bp
+++ b/hal/effects/qcom-effects/offloadbundle/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libqcompostprocbundle",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
diff --git a/hal/effects/qcom-effects/offloadvisualizer/Android.bp b/hal/effects/qcom-effects/offloadvisualizer/Android.bp
index e118d5487..43dd776ab 100644
--- a/hal/effects/qcom-effects/offloadvisualizer/Android.bp
+++ b/hal/effects/qcom-effects/offloadvisualizer/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libqcomvisualizer",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
diff --git a/hal/effects/qcom-effects/voiceprocessing/Android.bp b/hal/effects/qcom-effects/voiceprocessing/Android.bp
index 1249be0aa..c55cc0065 100644
--- a/hal/effects/qcom-effects/voiceprocessing/Android.bp
+++ b/hal/effects/qcom-effects/voiceprocessing/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libqcomvoiceprocessing",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
diff --git a/hal/effects/qcom-effects/volumelistener/Android.bp b/hal/effects/qcom-effects/volumelistener/Android.bp
index 6c8a61cc4..627efdf30 100644
--- a/hal/effects/qcom-effects/volumelistener/Android.bp
+++ b/hal/effects/qcom-effects/volumelistener/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libvolumelistener",
     defaults: ["aidlaudioeffectserviceqti_defaults"],
     owner: "qti",
diff --git a/hal/fuzz/Android.bp b/hal/fuzz/Android.bp
index 7a2df2526..498d5ff0f 100644
--- a/hal/fuzz/Android.bp
+++ b/hal/fuzz/Android.bp
@@ -1,4 +1,10 @@
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "fuzz-audio-hal",
     defaults: [
         "latest_android_hardware_audio_core_ndk_shared",
diff --git a/hal/service/Android.bp b/hal/service/Android.bp
index 7fabd50a0..7651e4950 100644
--- a/hal/service/Android.bp
+++ b/hal/service/Android.bp
@@ -1,4 +1,10 @@
 cc_binary {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "audiohalservice.qti",
     vendor: true,
     relative_install_path: "hw",
