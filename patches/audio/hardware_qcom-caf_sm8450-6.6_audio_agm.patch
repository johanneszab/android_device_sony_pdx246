diff --git a/ipc/aidl/aidlconverter/Android.bp b/ipc/aidl/aidlconverter/Android.bp
index f5a82a1a..55347a25 100644
--- a/ipc/aidl/aidlconverter/Android.bp
+++ b/ipc/aidl/aidlconverter/Android.bp
@@ -1,4 +1,10 @@
 cc_library_static {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagmaidltypeconverter",
     vendor: true,
 
diff --git a/ipc/aidl/client/Android.bp b/ipc/aidl/client/Android.bp
index d88d076b..888fb070 100644
--- a/ipc/aidl/client/Android.bp
+++ b/ipc/aidl/client/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagmclient",
     owner: "qti",
     vendor: true,
diff --git a/ipc/aidl/server/Android.bp b/ipc/aidl/server/Android.bp
index dab8f0ae..88dd6bd6 100644
--- a/ipc/aidl/server/Android.bp
+++ b/ipc/aidl/server/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagmipcservice",
     owner: "qti",
     vendor: true,
diff --git a/plugins/tinyalsa/Android.bp b/plugins/tinyalsa/Android.bp
index 6188abd4..a38bc307 100644
--- a/plugins/tinyalsa/Android.bp
+++ b/plugins/tinyalsa/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagm_pcm_plugin",
     defaults: [
         "qtiaudio_dynamic_log_defaults",
@@ -26,6 +32,12 @@ cc_library_shared {
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagm_mixer_plugin",
     defaults: [
         "qtiaudio_dynamic_log_defaults",
@@ -51,6 +63,12 @@ cc_library_shared {
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagm_compress_plugin",
     defaults: [
         "qtiaudio_dynamic_log_defaults",
diff --git a/service/Android.bp b/service/Android.bp
index 8d5a9189..83d3b245 100644
--- a/service/Android.bp
+++ b/service/Android.bp
@@ -1,10 +1,22 @@
 cc_library_headers {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagm_headers",
     vendor: true,
     export_include_dirs: ["inc/public"],
 }
 
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libagm",
     defaults: [
         "qtiaudio_dynamic_log_defaults",
diff --git a/snd_parser/Android.bp b/snd_parser/Android.bp
index 2d0eac91..b1931bee 100644
--- a/snd_parser/Android.bp
+++ b/snd_parser/Android.bp
@@ -1,4 +1,10 @@
 cc_library_shared {
+    // 64-bit only for device/sony/pdx246: this tree is compiled against the
+    // kernel UAPI headers, which LineageOS exports as arm64 for BOTH arches,
+    // so a 32-bit build picks up arm64 asm/sigcontext.h and fails on
+    // __uint128_t. pdx246 has a 2nd arch (stock is zygote64_32); pdx257 is
+    // 64-bit only and never hits this. See PORTING-NOTES.md "AUDIO".
+    compile_multilib: "64",
     name: "libsndcardparser",
     owner: "qti",
     vendor: true,
