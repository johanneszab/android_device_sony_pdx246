diff --git a/gpt-utils/Android.bp b/gpt-utils/Android.bp
index 7a845a3..8bd9fa1 100644
--- a/gpt-utils/Android.bp
+++ b/gpt-utils/Android.bp
@@ -6,7 +6,13 @@
 cc_library {
     name: "libgptutils.qti",
     export_include_dirs: ["."],
-    header_libs: ["generated_kernel_headers"],
+    // The UFS BSG framework headers (scsi/scsi_bsg_ufs.h, linux/bsg.h) come
+    // with bionic, so only the legacy QTI UFS ioctl path needs the headers
+    // generated from the kernel source.
+    header_libs: select(soong_config_variable("QTI_GPT_UTILS", "USE_BSG_FRAMEWORK"), {
+        false: ["generated_kernel_headers"],
+        default: [],
+    }),
     recovery_available: true,
     shared_libs: [
         "libcutils",
